plugins {
    id 'java'
}

group = 'br.com.makersweb.makersfood.application'
version = '1.0.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(":domain"))
    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}
